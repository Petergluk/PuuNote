# PuuNote — TODO Plan (2026-04-30)

> Источник: Consolidated Audit (Antigravity + Kimi k2.5)  
> Всего задач: 45 | Оценка: ~4-6 недель

---

## 🔴 Фаза 0 — Стабилизация (эта неделя, ~2-3 дня)

- [ ] **[P-00]** `FloatingCardActions`: убрать утечку portal + event listeners  
  `FloatingCardActions.tsx:129-186` — портал в `useRef`, cleanup в каждом `useEffect`

- [ ] **[P-00b]** `createNewFile`: `setState` вызывать только после успешной записи в DB  
  `useFileSystem.ts:279-287` — сейчас UI обновляется даже при failed DB write

- [ ] **[P-02]** `clearCompleted` в JobStore: добавить `cancelled` в фильтр  
  `useJobStore.ts:57` — одна строка

- [ ] **[P-07]** `historySlice`: добавить `.slice(0, 50)` для массива `future`  
  `historySlice.ts:59` — одна строка

- [ ] **[P-27]** `closeConfirm`: обнулять `onConfirm` при закрытии диалога  
  `uiSlice.ts:22` — `{ isOpen: false, message: "", onConfirm: undefined }`

- [ ] **[C5]** `utils/id.ts`: убрать пустой `catch {}` → lint fix  
  Добавить комментарий или логировать в dev

---

## 🟠 Фаза 1 — Критические баги (1-2 недели)

- [ ] **[H1]** `SnapshotPanel`: race condition — `fileId` захватывается в closure  
  `SnapshotPanel.tsx:23-34` — передавать `fileId` как параметр в момент вызова

- [ ] **[H3]** `normalizeSiblingOrder`: мутирует входные объекты — сделать чистой  
  `documentTree.ts:15-22` — `siblings.map((s, i) => ({ ...s, order: i }))`

- [ ] **[P-04]** `updatedAt`: унифицировать тип `string → number` в Dexie  
  `documentService.ts:36` — потребует миграцию DB версии 3

- [ ] **[P-05]** `parseJsonExport`: валидировать импортируемые ноды через Zod  
  `documentExport.ts:81` — `PuuNodesArraySchema.parse(parsed.nodes)`

- [ ] **[P-06]** `migrateLegacyLocalStorage`: обернуть `JSON.parse` в try/catch  
  `documentService.ts:72`

- [ ] **[P-10]** `handleGlobalPaste`: учитывать `pasteSplitMode` при paste  
  `useAppHotkeys.ts:29` — сейчас настройка игнорируется

---

## 🟡 Фаза 2 — Производительность (2-4 недели)

- [ ] **[H2]** `Card.tsx`: добавить `useShallow` к Zustand-селекторам  
  `Card.tsx:18-39` — `import { useShallow } from 'zustand/shallow'`

- [ ] **[H6]** `FloatingCardActions`: мемоизировать `canMergeNodes`  
  `FloatingCardActions.tsx:52` — `useMemo(() => canMergeNodes(...), [nodes, selectedIds])`

- [ ] **[H9]** `CommandPalette`: мемоизировать Fuse index, инвалидировать по `nodes`  
  `CommandPalette.tsx:71` — сейчас rebuild на каждый ⌘K

- [ ] **[P2]** `TimelineView`: исправить Virtuoso scroll wiring (window → container)  
  `TimelineView.tsx` — передать `scrollerRef` к контейнеру

- [ ] **[P-01]** Undo/Redo: перейти на patch-based или structural sharing  
  `historySlice.ts:38` — 50 полных копий дерева в памяти

- [ ] **[P-03]** `toggleSelection + Shift`: кешировать DFS-порядок  
  `selectionSlice.ts:18` — сейчас O(n) buildTreeIndex на каждый shift-click

- [ ] **[P-11]** `deleteNode`: использовать `childrenMap` вместо `.filter()` в цикле  
  `documentTree.ts:140` — O(n×d) → O(n)

- [ ] **[P-12]** `computeDescendantIds`: заменить `queue.shift()` на pointer-based BFS  
  `tree.ts:66` — `Array.shift()` = O(n²)

- [ ] **[P-13]** `moveNodes/isDescendant`: использовать `nodeMap` для O(1) lookup  
  `documentTree.ts:201`

- [ ] **[PF-10]** `buildContextForLLM`: заменить string concat на `chunks.join("")`  
  `contextExtraction.ts:137`

---

## 🟢 Фаза 3 — UX и i18n (1 месяц)

### AI
- [ ] **[H10]** AI-операции: обеспечить Undo через ⌘Z  
  `aiOperations.ts` — `setNodes` уже пишет в `past`, убедиться что AI-apply идёт через него

### Модалки и взаимодействие
- [ ] **[M3]** `SnapshotPanel`: добавить `isRestoring` state при restore  
- [ ] **[M5]** `Header`: keyboard navigation в export dropdown (`role="menu"`, стрелки)  
- [ ] **[M9]** `FloatingCardActions`: keyboard trigger (Space на активной карточке)  
- [ ] **[M10]** Merge: добавить `openConfirm` перед `mergeNodes`  
- [ ] **[M11]** `Footer`: показывать "выбрано N" при `selectedIds.length > 1`  

### UX
- [ ] **[UX-3]** `Card`: улучшить визуальное различие active (cursor) vs selected (multi)  
- [ ] **[UX-4]** `SnapshotPanel`: сделать имена снапшотов редактируемыми  
- [ ] **[UX-5]** `Header`: добавить хоткей Ctrl+, для Settings  
- [ ] **[UX-6]** `Header`: добавить кнопку переключения темы (не только через ⌘K)  
- [ ] **[UX-7]** `ShortcutsModal`: закрытие по Escape  
- [ ] **[UX-8]** `useAppHotkeys`: Delete/Backspace для удаления активного узла  

### i18n
- [ ] **[P-14]** `SettingsPanel`: перевести все русские label через `t()`  
- [ ] **[P-15]** `TimelineView:207`: `"Уровень"` → `t("Level")`  
- [ ] **[P-17]** `AutoSizeTextarea:148`: fallback `"текст"` → `"text"`  
- [ ] **[P-18]** `AutoSizeTextarea:166`: `"ссылка"` → `"link"`  

### Мелочи
- [ ] **[P-09]** Вынести дублирующиеся prose-классы в общий CSS/компонент  
  (Card, FullScreenModal, TimelineView)
- [ ] **[P-19]** `ErrorBoundary`: добавить кнопку Reload  
- [ ] **[P-20]** `browserDownload`: вызывать `a.remove()` после click  
- [ ] **[P-21]** `FloatingCardActions`: исправить подсчёт потомков (прямые дети ≠ все)  
- [ ] **[P-24]** Снизить `AUTOSIZE_DEBOUNCE_MS` с 400 до 150ms  
- [ ] **[P-26]** Добавить `aria-label` к ключевым интерактивным элементам  

---

## ⚙️ Фаза 4 — Инфраструктура

- [ ] **Тесты**: unit-тесты для `documentTree`, `schema`, `markdownParser`, `contextExtraction`  
- [ ] **CI/CD**: GitHub Actions — Vitest + ESLint + TypeScript strict + build check  
- [ ] **PWA**: манифест + Service Worker для offline  
- [ ] **[S-03]**: Backend proxy для AI API-ключей (при подключении реального LLM)  
- [ ] **[S-04]**: `referrerpolicy="no-referrer"` на `<img>` в Markdown (приватность)  

---

## 🤖 Фаза 5 — AI Integration (следующий квартал)

- [ ] Реализовать `OpenAiProvider` / `AnthropicProvider`  
- [ ] UI для API-ключа и выбора провайдера в SettingsPanel  
- [ ] Streaming (SSE) для прогрессивной генерации  
- [ ] Backend proxy для API-ключей  
- [ ] Sandboxed plugin execution  

---

## Статистика

| Фаза | Задач | Сложность | Приоритет |
|------|-------|-----------|-----------|
| 0 — Стабилизация | 6 | Низкая | 🔴 Немедленно |
| 1 — Критические баги | 6 | Средняя | 🟠 Эта неделя |
| 2 — Производительность | 10 | Высокая | 🟡 2-4 недели |
| 3 — UX и i18n | 23 | Низкая-средняя | 🟢 1 месяц |
| 4 — Инфраструктура | 5 | Средняя | ⚙️ Параллельно |
| 5 — AI | 5 | Высокая | 🤖 Квартал |
| **Итого** | **55** | — | — |
